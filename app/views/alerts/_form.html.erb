<div  class="container_s" >
<%= form_for(@alert, html: { class: "form-horizontal", role: "form" }) do |f| %>
  <% if @alert.errors.any? %>
    <div class="alert alert-danger alert-dismissable" role="alert">
      <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
      <h4><%= pluralize(@alert.errors.count, "error") %> prohibited this alert from being saved:</h4>

      <ul>
      <% @alert.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div id="infoPanel">
    <div id="markerStatus"><i>Mueve el Marcador para apuntar el lugar que quieres.</i></div>    
  </div>
  <div id="mapCanvas" class="map"></div>


  
  <div class="form-group">
    <%= f.label :latitude, "Latitud", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :latitude, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :longitude, "Longitud", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :longitude, class: "form-control" %>
    </div>
  </div>
  <div class="form-group">
    <%= f.label :description, "Lugar", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_field :description, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :desc, "Descripción", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <%= f.text_area :desc, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :level, "Nivel de Peligro", class: "col-sm-2 control-label" %>
    <div class="col-sm-10">
      <select class="form-control" name="alert[level]" id="alert_level">
        <option value="1">Leve</option>
        <option value="2">Moderado</option>
        <option value="3">Peligroso</option>
      </select>
    </div>
  </div>
  <div class="form-group">
    <div class="col-sm-offset-2 col-sm-10">
      <%= f.submit "Crear Alerta", class: "btn btn-primary" %>
    </div>
  </div>

<% end %>
</div>